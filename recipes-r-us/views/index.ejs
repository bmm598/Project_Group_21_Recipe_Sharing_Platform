<%- include("partials/header.ejs") %>

<<<<<<< HEAD
<!-- search box -->
<div class="body-container">
    <div class="body-object">
        <form action="/" method="GET" id="search-form" class="form-container">
            <div class="form-object">
                <input
                    type="text"
                    name="q"
                    placeholder="Search recipes by title, description, or tags"
                    value="<%= typeof searchQuery !== 'undefined' ? searchQuery : '' %>"
                >
            </div>
            <div class="form-object">
                <input type="submit" value="Search">
            </div>
        </form>
    </div>
=======
<div class="search-bar-container">
    <form action="/search" method="post">   
        <div class="search-bar-object">
            <label for="search-input">Search:</label>
            <input type="search" id="search-input" name="q" placeholder="Enter your search term..." />
            <button type="submit">Search</button>
        </div>
    </form>
>>>>>>> ecd7c655409d54854f905d5c60c574ad049c1e40
</div>

<!-- if recipes list exists -->
<% if (locals.recipes) { %>
    <div class="recipe-container">
        <!-- for each recipe in recipes create this object -->
        <% recipes.forEach(recipe => { %>
            <div class="recipe-object">
                    <%= recipe.date_created %> 
                <h3> 
                    <%= recipe.title %> 
                </h3>
                <p> 
                    by: <%= recipe.creator_name %> 
                </p>
                <img src="<%= recipe.img %>" alt="Image of <%= recipe.title %>">
                <p> 
                    <%= recipe.body %> 
                </p>

                <!-- tags block -->
                <% if (recipe.tags) { %>
                    <div class="tag-container">
                        <% recipe.tags.split(",").forEach((tagRaw) => { 
                            const t = tagRaw.trim();
                            if (t.length === 0) return;
                        %>
                            <a
                                class="tag-badge"
                                href="/?tag=<%= encodeURIComponent(t) %><%= (typeof searchQuery !== 'undefined' && searchQuery.trim() !== '') ? '&q=' + encodeURIComponent(searchQuery) : '' %>"
                            >
                                <%= t %>
                            </a>
                        <% }) %>
                    </div>
                <% } %>

                <!-- if the current user is the owner of this post, then show edit and delete -->
                <% if(locals.loggedIn == true) { %>
                    <% if(locals.user[0].creator_id == recipe.creator_user_id) { %>
                    <div class="edit-object">
                        <form id="delete-form" action="<%= recipe.recipe_id %>/delete" method="POST">
                            <div class="form-object">
                                <input type="submit" value="Delete">
                            </div>
                        </form>
                        <form id="edit-form" action="<%= recipe.recipe_id %>/edit" method="GET">
                            <div class="form-object">
                                <input type="submit" value="Edit">
                            </div>
                        </form>
                <a href="<%= recipe.recipe_id %>/recipe">
                <div class="recipe-split">
                    <div class="recipe-img">
                        <img src="<%= recipe.img %>" alt="Image of <%= recipe.title %>">
                    </div>
                    <div class="recipe-description">
                        <div>
                            <%= recipe.date_created %> 
                        </div>
                        <h3> 
                            <%= recipe.title %> 
                        </h3>
                        <div>by: <%= recipe.creator_name %> </div>
                        <div class="star-rating" id="starRating">
                            <span class="star <%= recipe.avg_rating > 1? "checked" : null %>">&#9733;</span>
                            <span class="star <%= recipe.avg_rating > 2? "checked" : null %>">&#9733;</span>
                            <span class="star <%= recipe.avg_rating > 3? "checked" : null %>">&#9733;</span>
                            <span class="star <%= recipe.avg_rating > 4? "checked" : null %>">&#9733;</span>
                            <span class="star <%= recipe.avg_rating == 5? "checked" : null %>">&#9733;</span>
                        </div>
                        <p>
                            <%= recipe.body %> 
                        </p>
                        <!-- if the current user is the owner of this post, then show edit and delete -->
                        <% if(locals.loggedIn == true) { %>
                            <% if(locals.user[0].creator_id == recipe.creator_user_id) { %>
                            <div class="edit-object">
                                <form id="delete-form" action="<%= recipe.recipe_id %>/delete" method="POST">
                                    <div class="form-object">
                                        <input type="submit" value="Delete">
                                    </div>
                                </form>
                                <form id="edit-form" action="<%= recipe.recipe_id %>/edit" method="GET">
                                    <div class="form-object">
                                        <input type="submit" value="Edit">
                                    </div>
                                </form>
                            </div>
                            <% } %>
                        <% } %>
                    </div>
                </div>
                </a>
            </div>
        <% }); %>
    </div>
<% } %>

<!-- if user is logged in then allow them to draft a recipe post -->
<% if(locals.loggedIn == true) { %>
<div class="body-container">
        <div class="body-object">
            <h2>Draft a Recipe Post</h2>
        </div>
        <div class="body-object">
            <form id="recipe-form" action="/submit" method="POST">
                <div class="form-container">
                    <!-- title -->
                    <div class="form-object">
                        <input
                            type="text"
                            name="title"
                            placeholder="Recipe Title*"
                            required
                        >
                    </div>

                    <!-- creator -->
                    <div class="form-object">
                        <p>
                            by: <%= user[0].name %>
                        </p>
                    </div>

                    <!-- description -->
                    <div class="form-object">
                        <textarea
                            name="content"
                            placeholder="Short description or notes about the recipe*"
                            required
                        ></textarea>
                    </div>

                    <!-- ingredients -->
                    <div class="form-object">
                        <textarea
                            name="ingredients"
                            placeholder="Ingredients* (one per line)"
                            required
                        ></textarea>
                    </div>

                    <!-- instructions -->
                    <div class="form-object">
                        <textarea
                            name="instructions"
                            placeholder="Instructions*"
                            required
                        ></textarea>
                    </div>

                    <!-- tags & badges -->
                    <div class="form-object">
                        <input
                            type="text"
                            name="tags"
                            placeholder="Tags (comma-separated, e.g. vegan, breakfast, spicy)"
                        >
                    </div>

                    <!-- diet preference -->
                    <div class="form-object">
                        <input
                            type="text"
                            name="diet"
                            placeholder="Dietary preference (e.g. Vegan, Gluten-free)"
                        >
                    </div>

                    <!-- cook time -->
                    <div class="form-object">
                        <input
                            type="number"
                            name="cook_time"
                            min="0"
                            placeholder="Cooking time (minutes)"
                        >
                    </div>

                    <!-- difficulty -->
                    <div class="form-object">
                        <select name="difficulty">
                            <option value="">Select difficulty</option>
                            <option value="Easy">Easy</option>
                            <option value="Medium">Medium</option>
                            <option value="Hard">Hard</option>
                        </select>
                    </div>

                    <!-- image url -->
                    <div class="form-object">
                        <input
                            type="url"
                            name="img"
                            placeholder="Image URL (optional)"
                        >
                    </div>

                    <!-- submit -->
                    <div class="form-object">
                        <input type="submit" value="Submit Recipe">
                    </div>
                </div>
            </form>
        </div>
</div>
<% } %>

<%- include("partials/footer.ejs") %>